@using HayleesThreads.ViewModels
@model ShoppingCartViewModel

<span class="z" style="color:#e47911"> @Model.ShoppingCart.ShoppingCartProducts.Count</span>

@* if(Model.ShoppingCart.ShoppingCartProducts.Count > 0)
{
  <li>
    <a>
      <span class="glyphicon glyphicon-shopping-cart"></span>
      <span id="cart-status">
        @Model.ShoppingCart.ShoppingCartProducts.Count
      </span>
    </a>
  </li>
} *@